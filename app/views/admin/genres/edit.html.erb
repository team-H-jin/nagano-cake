<div class='container'>
  <div class='row'>
    <div class='mx-auto'>
     <h4 class='bg-dark text-light'>ジャンル編集</h4></br>
      <div class='row'> 
       ジャンル名　　
       <%= form_with model: [:admin,@genre],  method: :patch do |f| %>
       <%= f.text_field :name %>　　
       <%= f.submit '変更を保存', class: 'btn btn-success rounded-pill'%>
       <% end %> 
      </div>
    </div>
  </div>
</div>       