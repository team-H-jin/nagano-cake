<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
     <h4>ジャンル編集</h4>
      <div>ジャンル名</div>
       <%= form_with model: [:admin,@genre],  method: :patch do |f| %>
       <%= f.text_field :name %>
       <%= f.submit '変更を保存', class: 'btn btn-success'%>
       <% end %> 
    </div>
  </div>
</div>       