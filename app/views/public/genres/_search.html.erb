 <div class="container">
    <div class="row">
        <div class="col-lg-3">
.         <table class="table table-bordered">
            <tr><th>ジャンル検索</th></tr>
            <% @genres.each do |genre| %>
            <tr><td><%= link_to genre.name,genre_path(genre) %></td></tr>
            <% end %>
          </table>
        </div>
    </div>
 </div>