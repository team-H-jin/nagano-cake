<div class="container">
  <div class="row">
    <div class="col-lg-1"></div>
    <div class="col-lg-10">
      <br><h4>　　<u>注文一覧</u></h4><br>
      <table class="table table-bordered">
        <thead>
          <tr class="table-info">
            <th>注文日</th>
            <th>配送先</th>
            <th>注文商品</th>
            <th>支払金額</th>
            <th>ステータス</th>
            <th>注文詳細</th>
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |order| %>
          <tr>
            <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
            <td>
              〒<%= order.posecode %><br>
    		      <%= order.address %><br>
    		      <%= order.name %>
            </td>
            <td>
              <% order.order_details.each do |order_detail| %>
    		      <%= order_detail.item.name %><br>
    		      <% end %>
            </td>
            <td><%= total.add_tax_price.to_s(:dalimited) %></td>
            <td><%= order.status %></td>
            <td>
              <%= link_to '表示する', orders_path, class: "btn btn-sm btn-primary rounded-pill" %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-lg-1"></div>
  </div>
</div>